services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: funnel
      POSTGRES_USER: funnel
      POSTGRES_PASSWORD: funnel
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes: { pgdata: {} }